---
"evlog": minor
---

`evlog/eve` 集成现在会记录每个工具结果新增了多少上下文。`ai.tools[]` 为每个工具新增了 `inputTokens` 字段，其值根据调用工具的步骤与使用其结果的步骤之间的输入令牌增量计算，因此无需手动进行前后差异比较，就能显示出在一轮处理中占据主要上下文的工具。在同一个步骤中的并行工具结果会均分该增量；如果某个结果的增量跨越了压缩或上下文重置，则会留空，而不会被错误归因。
