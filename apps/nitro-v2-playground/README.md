# Nitro v2 evlog playground

Demonstrates evlog integration with **Nitro v2** (`nitropack` + `h3`) using the `evlog/nitro` module.

## Getting started

```bash
pnpm install
pnpm run dev
```

## Deploying

```bash
pnpm run build
```
