---
title: evlog 与 pino、winston、consola 对比
description: evlog 与 pino、winston 和 consola 的并排比较。功能对照矩阵、诚实的差距，以及迁移示例，帮助你无缝切换而不会有意外。
navigation:
  title: 与其他日志库对比
  icon: i-lucide-scale
links:
  - label: 性能基准
    icon: i-lucide-gauge
    to: /core-concepts/performance
    color: neutral
    variant: subtle
  - label: 简单日志记录
    icon: i-lucide-terminal
    to: /logging/simple-logging
    color: neutral
    variant: subtle
---

evlog 首先是一个功能完备的通用日志记录器，并将 wide events 作为同一 API 的原生扩展。本文将它与 TypeScript 开发者通常会考虑的三个日志库——**pino**、**winston** 和 **consola**——进行正面对比，这样你就能清楚地知道你会得到什么、哪些保持不变，以及目前缺少什么（如果有的话）。

## TL;DR

- **如果你想要与 pino 相同级别的吞吐量，同时内置结构化错误、脱敏和 wide events，请选择 evlog 而不是 pino**——而且你不想自己拼装 `pino` + `pino-pretty` + `pino-http` + 自定义 transports。
- **在任何新的 TypeScript 项目中都请选择 evlog 而不是 winston**。winston 更老、更慢（见 [benchmarks](/core-concepts/performance)），并且不具备任何现代特性（类型化事件、脱敏、结构化错误、AI SDK 集成）。
- **如果你的代码已经离开 CLI 环境，就请选择 evlog 而不是 consola**。consola 非常适合终端的美化输出，但它不提供 drain pipeline、采样或 wide events。
- **只有在你处于一个极其热的路径上，并且每秒向 `/dev/null` 发出数十万条一次性日志，同时你还有一个不想迁移的自定义 transport 时，才继续使用 pino**。evlog 在 wide event 生命周期上仍然以 7.7 倍获胜，但在纯粹的 `info('hello world')` 吞吐量上，pino 可能略胜一筹。

## 功能对比

用三张表代替一大块内容。右侧的 **Winner** 列让你一眼就能看出每一行的赢家；单元格使用语义化词语（“内置”、“手动”、“通过 X”）而不是通用的“是”，这样你无需阅读规范就能看出实现成本。

::callout{icon="i-lucide-info" color="info"}
将鼠标悬停（或在移动端轻触）在任何功能名称旁边的 **信息图标** 上，即可查看一行说明，了解该行实际比较的内容。
::

### 核心 API

| 功能 | evlog | pino | consola | winston | 赢家 |
|---------|:-----:|:----:|:-------:|:-------:|:-------|
| :feature-label[标准级别]{tip="每个日志库都会提供的四种经典严重级别（debug / info / warn / error），用于基于严重程度的过滤。"} | 是 | 是 | 是 | 是 | 全部 |
| :feature-label[自定义级别]{tip="定义超出标准四级之外的额外严重级别，例如 trace、notice 或 fatal——在从依赖这些级别的流水线迁移时很有用。"} | 否 | 是 | 是 | 是 | **pino、consola、winston** |
| :feature-label[每次调用的结构化字段]{tip="在每次日志调用中传递类型化的键值对象，而不是把数据塞进消息字符串里。对于可查询日志是必需的。"} | 是 | 是 | 部分 | 是 | **evlog、pino、winston** |
| :feature-label[子日志记录器 / 持久绑定]{tip="创建一个继承父级绑定（例如 requestId、component）的日志记录器，并自动将它们附加到后续每次调用中。"} | 是 | 是 | 是 | 是 | 全部 |
| :feature-label[开发时美化 / 生产时 JSON（自动）]{tip="开发时输出人类可读的彩色内容，生产环境输出机器可解析的 JSON。根据 NODE_ENV 自动切换。"} | 内置 | 通过 pino-pretty | 内置 | 手动 | **evlog、consola** |
| :feature-label[浏览器安全构建]{tip="专为浏览器提供的独立运行时，会剥离 Node.js API（process、fs），并提供更小的客户端日志打包体积。"} | 是 | 否 | 是 | 否 | **evlog、consola** |
| :feature-label[子操作日志记录器（log.fork）]{tip="从父 wide event 中分出一个子事件——用于批量操作或在一次请求中对每个项目进行处理，同时不丢失关联性。"} | 是 | 否 | 否 | 否 | **evlog** |
| :feature-label[来源区分（服务端 / 客户端）]{tip="每个事件都会自动携带 source 字段（server 或 client），这样你可以在仪表板中把客户端错误和服务端错误分开。"} | 是 | 否 | 否 | 否 | **evlog** |
| :feature-label[运行时级别变更]{tip="无需重启即可动态切换日志级别（例如 logger.level = 'debug'）——适用于 --verbose 标志或热重载。"} | 否 | 是 | 是 | 是 | **pino、consola、winston** |
| :feature-label[插件 / 序列化器系统]{tip="可插拔转换器，可将类型化对象（错误、请求、响应）转换为 JSON。evlog 通过脱敏 + createError 处理常见场景，而不是依赖插件。"} | 否 | 是 | 否 | 是 | **pino、winston** |
| :feature-label[wide events（每个操作一个）]{tip="在整个请求过程中累积上下文，并在最后发出一个类型化事件——这是来自 Honeycomb / Charity Majors 的 wide-event 可观测性模式。"} | 是 | 否 | 否 | 否 | **evlog** |
| :feature-label[结构化错误（why / fix / link）]{tip="错误会携带 why（根因）、fix（可执行的下一步）和 link（文档 URL）字段，并从服务端传递到客户端。"} | 是 | 否 | 否 | 否 | **evlog** |

### 生产特性

| 功能 | evlog | pino | consola | winston | 赢家 |
|---------|:-----:|:----:|:-------:|:-------:|:-------|
| :feature-label[内置 PII 脱敏（生产环境自动）]{tip="在生产输出中自动屏蔽 password、token、email、creditCard 等字段。可通过 redact 列表配置。"} | 内置 | 手动 | 否 | 否 | **evlog** |
| :feature-label[头部 + 尾部采样]{tip="在发出时丢弃一定比例的低重要性事件（头部采样），或根据最终结果（如错误 / 慢请求）进行丢弃（尾部采样）。"} | 内置 | 手动 | 否 | 否 | **evlog** |
| :feature-label[用于传输日志的异步 I/O]{tip="日志传输是否脱离热路径。evlog 使用 fire-and-forget drains（在边缘环境中使用 waitUntil）；pino / winston 使用 worker thread 实现强隔离。"} | 通过 drains | Worker thread | 否 | Worker thread | **pino、winston** |
| :feature-label[Drain pipeline（批处理 / 重试 / 扇出）]{tip="内置批处理、失败时指数退避重试，以及向多个目标扇出——无需你自己编写 transport 管道。"} | 内置 | 通过 transports | 否 | 通过 transports | **evlog** |
| :feature-label[多目标扇出]{tip="将同一个事件并行发送到多个目标（例如 Axiom + Sentry + 你的数据库），而无需重复发出调用。"} | 是 | 是 | 否 | 是 | **evlog、pino、winston** |
| :feature-label[审计日志（防篡改链）]{tip="针对安全敏感事件（认证、计费、管理员操作）的仅追加日志，并带有哈希链，因此事后可以检测篡改。"} | 内置 | 否 | 否 | 否 | **evlog** |
| :feature-label[内置丰富器（UA / Geo / Trace / Size）]{tip="自动附加派生上下文，如解析后的 user-agent、GeoIP、请求大小，以及 W3C traceparent，而无需编写中间件。"} | 内置 | 否 | 否 | 否 | **evlog** |
| :feature-label[敏感请求头过滤]{tip="在进入适配器之前移除 authorization、cookie、x-api-key、set-cookie 等内容——不会有把认证令牌泄露给可观测性供应商的风险。"} | 内置 | 手动 | 否 | 手动 | **evlog** |
| :feature-label[W3C trace 上下文（traceparent）]{tip="读取传入的 W3C traceparent / tracestate 请求头，并通过 wide event 传播它们，以便进行分布式追踪关联。"} | 内置 | 否 | 否 | 否 | **evlog** |
| :feature-label[AI SDK 集成（token / tools / streaming）]{tip="面向 Vercel AI SDK 的即插即用中间件，可自动跟踪 token 使用量、tool 调用、流式指标以及每次 LLM 调用的成本。"} | 内置 | 否 | 否 | 否 | **evlog** |
| :feature-label[Better Auth 集成]{tip="为 Better Auth 提供一行式插件，可针对登录、注册、密码重置、会话刷新和撤销发出 wide events。"} | 内置 | 否 | 否 | 否 | **evlog** |
| :feature-label[自托管存储（NuxtHub 适配器）]{tip="将日志存储在你自己的 NuxtHub 托管数据库中，并自动保留——无需外部可观测性供应商。"} | 内置 | 否 | 否 | 否 | **evlog** |
| :feature-label[Edge / Workers 运行时]{tip="对 Cloudflare Workers 和 Vercel Edge 提供一等支持，并集成 waitUntil()，确保 drain 在运行时终止前完成。"} | 内置 | 部分 | 否 | 否 | **evlog** |

### 占用与生态

| 功能 | evlog | pino | consola | winston | 赢家 |
|---------|:-----:|:----:|:-------:|:-------:|:-------|
| :feature-label[零传递依赖]{tip="evlog 本身不包含任何生产依赖。适配器仅在被导入时才懒加载其供应商 SDK。"} | 是 | 1 个依赖 | 否 | 否 | **evlog** |
| :feature-label[打包体积（gzip）]{tip="导入到服务端 bundle 中时核心日志库的大致 gzip 后体积。对于无服务器环境的冷启动时间来说，越小越好。"} | ~6 kB | ~6 kB | ~12 kB | ~50 kB | **evlog** |
| :feature-label[wide event 生命周期吞吐量]{tip="日志记录器在单核上每秒能完成多少个完整请求生命周期（start → set/set/set → emit）。方法见 packages/evlog/bench/。"} | 1.58M ops/s | 206K ops/s | n/a | 112K ops/s | **evlog** |
| :feature-label[框架自动初始化（13+ 集成）]{tip="适用于 Nuxt、Nitro、Next.js、SvelteKit、TanStack Start、React Router、NestJS、Express、Hono、Fastify、Elysia、Cloudflare Workers 和 Vercel Edge 的一行式安装。"} | 是 | 仅 HTTP | 否 | 否 | **evlog** |
| :feature-label[客户端 → 服务端日志传输]{tip="浏览器端日志记录器会批量处理客户端事件，并通过 HTTP 将它们发送到你的服务器，在那里它们会合并到 wide event 生命周期中。"} | 是 | 否 | 否 | 否 | **evlog** |
| :feature-label[Vite 插件（自动替换 console.log）]{tip="构建时将 console.log → log.info 重写，因此你无需手动编辑每个文件就能采用 evlog。"} | 是 | 否 | 否 | 否 | **evlog** |
| :feature-label[路径过滤（包含 / 排除 glob）]{tip="使用 glob 模式完全跳过对 /health 或 /metrics 等路径的日志记录——在不添加中间件的情况下保持低噪音。"} | 内置 | 手动 | 否 | 手动 | **evlog** |
| :feature-label[AI 代理技能（Cursor / Claude / ChatGPT）]{tip="随附的 .skill.md 文件让 AI 编码代理按正确风格编写 evlog 代码，而无需你每次都教它们。"} | 是 | 否 | 否 | 否 | **evlog** |

在这三张表中合计（共 33 行）：evlog 直接赢得 **23** 行，平局 **6** 行，输掉 **4** 行——自定义级别、运行时级别变更、插件/序列化器系统，以及基于 worker thread 的异步 I/O。所有这四项失利都在下面的 [诚实差距](#honest-gaps-today) 中有说明，这样你就知道自己在权衡什么。

有关这些吞吐量数字背后的开源基准测试，请参见 [`packages/evlog/bench/`](https://github.com/hugorcd/evlog/tree/main/packages/evlog/bench)；完整拆解请查看 [性能页面](/core-concepts/performance)。

## 诚实的空白（当前）

我们宁愿你先读这份清单，也不希望你用踩坑的方式才发现这些限制。每一项都可能是未来的 Linear 任务——但目前它们都还没有阻塞我们已经发布 evlog 的工作负载。

### `log.*` 没有持久绑定简写

pino 有 `log.child({ component: 'auth' })`，它会返回一个新的 logger，同时继承父级的 bindings 和子级的 bindings。evlog 简单的 `log.*` API 是全局的；如果要附加持久上下文，你需要创建一个宽事件 logger：

```typescript
import { createLogger } from 'evlog'

const log = createLogger({ component: 'auth' })
log.set({ userId: 42 })
log.emit()
```

或者，在框架集成中，请求中间件会帮你做这件事。这个方案可行，但它和 `pino.child` 的易用形态并不完全一样。`log.child(bindings)` 这样的简写很可能是下一步会加入的功能。

### `minLevel` 在启动时只设置一次

你在 `initLogger({ minLevel: 'info' })` 中配置 `minLevel`，之后它在整个进程生命周期里都不会再变。pino 允许你在运行时修改 `logger.level = 'debug'`（对 `--verbose` 标志或热重载很有用）。目前的替代方案是在锁定之前再次调用 `initLogger`——对于在任何日志输出之前先读取标志位的 CLI 来说没问题，但对于运行时切换就比较麻烦。

### 不支持自定义级别

evlog 只提供 `debug` / `info` / `warn` / `error`，仅此而已。pino、consola 和 winston 都允许你定义 `trace`、`notice`、`fatal` 等级别。我们是有意只选择四个级别的（大多数团队从来不会用超过四个），但如果你现有的流水线依赖 `fatal` 或 `trace`，你需要把它们映射到最接近的 evlog 级别。

### `log.*` 不支持多流 / transport 数组

pino 允许你通过 `pino.multistream` 将单条日志同时写入多个目的地。evlog 通过[排水管线](/adapters/building-blocks/pipeline)也能做到同样的事（一个 drain 分发到 N 个 adapter，并在所有 adapter 之间共享批处理和重试）——但心智模型不同。如果你现有代码是围绕“流 A 输出 debug 到 stdout，流 B 输出 warn+ 到文件，流 C 输出 error 到 Sentry”来组织的，那么你需要改为在 drain 层进行路由。

### 没有 formatter / serializer 插件系统

pino 有 `serializers`，用于把常见类型（错误、请求、响应）转换为 JSON。evlog 通过脱敏层和内置错误序列化（`createError` + `parseError`）处理常见情况；对于任何自定义需求（例如屏蔽某个特定字段或转换载荷），你需要在自定义 drain 中实现，或者在调用 `log.set` 之前处理。

## 从以下库迁移

请选择与你当前 logger 相匹配的标签页。每个标签页展示的是该库自身 API 下的 **迁移前** 代码。标签页下面是唯一的 **迁移后** 代码片段——无论你来自哪里，evlog 代码都相同。

::code-group

```typescript [pino]
import pino from 'pino'

const log = pino({ name: 'checkout' })
const child = log.child({ flow: 'checkout' })

child.info({ event: 'checkout_started' })

try {
  const cart = await getCart(userId)
  child.info({ cart: { items: cart.items.length, total: cart.total } }, 'cart loaded')

  const charge = await stripe.charge(cart.total)
  child.info({ stripe: { chargeId: charge.id } }, 'charge ok')

  if (!charge.success) {
    throw new Error(`Payment failed: ${charge.decline_reason}`)
  }
} catch (err) {
  child.error({ err }, 'checkout failed')
  throw err
}
```

```typescript [winston]
import { createLogger as createWinston, format, transports } from 'winston'

const log = createWinston({
  defaultMeta: { service: 'checkout' },
  format: format.combine(format.timestamp(), format.json()),
  transports: [new transports.Console()],
})

log.info({ event: 'checkout_started', flow: 'checkout' })

try {
  const cart = await getCart(userId)
  log.info({ flow: 'checkout', cart: { items: cart.items.length, total: cart.total } })

  const charge = await stripe.charge(cart.total)
  log.info({ flow: 'checkout', stripe: { chargeId: charge.id } })

  if (!charge.success) {
    throw new Error(`Payment failed: ${charge.decline_reason}`)
  }
} catch (err) {
  log.error({ flow: 'checkout', err })
  throw err
}
```

```typescript [consola]
import { consola } from 'consola'

const log = consola.withTag('checkout')

log.info('开始结账流程')

try {
  const cart = await getCart(userId)
  log.info('购物车已加载', { items: cart.items.length, total: cart.total })

  const charge = await stripe.charge(cart.total)
  log.info('扣款成功', { chargeId: charge.id })

  if (!charge.success) {
    throw new Error(`Payment failed: ${charge.decline_reason}`)
  }
} catch (err) {
  log.error('结账失败', err)
  throw err
}
```

```typescript [console.log]
console.log('[checkout] 开始结账流程')

try {
  const cart = await getCart(userId)
  console.log('[checkout] 购物车已加载', { items: cart.items.length, total: cart.total })

  const charge = await stripe.charge(cart.total)
  console.log('[checkout] 扣款成功', { chargeId: charge.id })

  if (!charge.success) {
    throw new Error(`Payment failed: ${charge.decline_reason}`)
  }
} catch (err) {
  console.error('[checkout] 失败', err)
  throw err
}
```

::

这四种最终都会变成这样——无论来源库是什么，代码都相同：

```typescript [After (evlog)]
import { initLogger, createLogger, createError } from 'evlog'

initLogger({ env: { service: 'checkout' } })

const log = createLogger({ flow: 'checkout' })

try {
  const cart = await getCart(userId)
  log.set({ cart: { items: cart.items.length, total: cart.total } })

  const charge = await stripe.charge(cart.total)
  log.set({ stripe: { chargeId: charge.id } })

  if (!charge.success) {
    throw createError({
      message: '支付失败',
      status: 402,
      why: charge.decline_reason,
      fix: '尝试其他支付方式',
    })
  }
} catch (err) {
  log.error(err as Error)
  throw err
} finally {
  log.emit()
}
```

每次迁移都会发生三件事：

- **N 条日志 → 1 条宽事件。** 每个请求中的 3-4 次调用会变成 `log.set` 的累积，以及最后一次 `log.emit`。你的仪表盘会得到一行可查询记录，而不是按 request id 拼接。
- **错误携带 `why` 和 `fix`。** 使用 `createError` 而不是 `new Error`，意味着你的客户端（以及值班人员）能拿到可执行的上下文，而不只是堆栈信息。
- **配置只需一行。** 不需要 formatter 接线，不需要 transport 组装，也不需要 `pino-pretty` 的 peer 依赖。启动时调用一次 `initLogger` 就完成了。

## 反向方向：什么时候不该选择 evlog

对自己诚实一点。如果出现以下情况，就不要切换：

- 你发布的是一个已经属于 pino 生态的库（`pino-http`、`pino-pretty`、`pino-multi-stream` 插件），并且不想失去这些工具链。
- 你有自定义的 pino transport（例如你在 2021 年写的一个 worker-thread Datadog 转发器），不想把它重新实现成 evlog drain。大多数[内置适配器](/adapters/overview)已经覆盖了常见目的地，但自定义协议就意味着需要迁移。
- 你只在 CLI 中记录日志，并且使用 consola 只是为了漂亮的终端输出。evlog 的美观输出已经不错，但在 spinner、prompt 和 box render 方面还不如 consola。两者都用：evlog 负责输出到 drain 的事件，consola 负责 prompts / TUI。

## 下一步

- [简单日志](/logging/simple-logging) — `log.*` API、迁移标签页和使用模式
- [宽事件](/logging/wide-events) — 按操作累积上下文后能解锁什么能力
- [性能基准](/core-concepts/performance) — 上面这些数字背后的测试方法
- [独立 TypeScript](/frameworks/standalone) — 没有 Web 框架时的脚本、worker 和库
