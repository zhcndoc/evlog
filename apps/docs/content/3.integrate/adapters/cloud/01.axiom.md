---
title: Axiom 适配器
description: 将广泛事件发送到 Axiom，以进行强大的查询、仪表板和告警。零配置设置，使用环境变量和自动批处理。
navigation:
  title: Axiom
  icon: i-custom-axiom
links:
  - label: Axiom 仪表板
    icon: i-lucide-external-link
    to: https://app.axiom.co
    target: _blank
    color: neutral
    variant: subtle
  - label: OTLP 适配器
    icon: i-simple-icons-opentelemetry
    to: /integrate/adapters/cloud/otlp
    color: neutral
    variant: subtle
---

[Axiom](https://axiom.co) 是一个具有强大查询能力的云原生日志平台。evlog Axiom 适配器会将你的广泛事件直接发送到 Axiom 数据集。

::prompt
---
description: 添加 Axiom 排水适配器
icon: i-custom-axiom
actions:
  - copy
  - cursor
  - windsurf
---

添加 Axiom 排水适配器，将 evlog 广泛事件发送到 Axiom。

1. 识别我正在使用的框架，并遵循其 evlog 集成模式
2. 如果尚未安装，请安装 evlog
3. 从 'evlog/axiom' 导入 createAxiomDrain
4. 将 createAxiomDrain() 接入我框架的 drain 配置
5. 在 .env 中设置 AXIOM_API_KEY 和 AXIOM_DATASET 环境变量
6. 通过触发一个请求并检查 Axiom 数据集进行测试

适配器文档：https://www.evlog.dev/integrate/adapters/cloud/axiom
框架设置：https://www.evlog.dev/integrate/frameworks/overview

::

## 安装

Axiom 适配器随 evlog 一起打包提供：

```typescript [src/index.ts]
import { createAxiomDrain } from 'evlog/axiom'
```

## 快速开始

### 1. 获取你的 Axiom 凭证

1. 创建一个 [Axiom 账户](https://app.axiom.co)
2. 创建一个用于日志的数据集
3. 生成一个具有写入权限的 API 令牌

### 2. 设置环境变量

```bash [.env]
AXIOM_API_KEY=xaat-your-token-here
AXIOM_DATASET=your-dataset-name
```

::callout{icon="i-lucide-info" color="info"}
在 Axiom 的仪表板中，这个凭证称为 **API 令牌**（`xaat-...`）。evlog 将配置字段命名为 `apiKey`，以便在各个适配器之间保持一致。旧的 `token` / `AXIOM_TOKEN` 仍然可用，直到下一个主要版本发布。
::

### 3. 将 drain 接入你的框架

::code-group
```typescript [Nuxt / Nitro]
// server/plugins/evlog-drain.ts
import { createAxiomDrain } from 'evlog/axiom'

export default defineNitroPlugin((nitroApp) => {
  nitroApp.hooks.hook('evlog:drain', createAxiomDrain())
})
```
```typescript [Next.js]
// lib/evlog.ts
import { createEvlog } from 'evlog/next'
import { createAxiomDrain } from 'evlog/axiom'

export const { withEvlog, useLogger, log, createError } = createEvlog({
  service: 'my-app',
  drain: createAxiomDrain(),
})
```
```typescript [Hono]
import { createAxiomDrain } from 'evlog/axiom'

app.use(evlog({ drain: createAxiomDrain() }))
```
```typescript [Express]
import { createAxiomDrain } from 'evlog/axiom'

app.use(evlog({ drain: createAxiomDrain() }))
```
```typescript [Fastify]
import { createAxiomDrain } from 'evlog/axiom'

await app.register(evlog, { drain: createAxiomDrain() })
```
```typescript [Elysia]
import { createAxiomDrain } from 'evlog/axiom'

app.use(evlog({ drain: createAxiomDrain() }))
```
```typescript [NestJS]
import { createAxiomDrain } from 'evlog/axiom'

EvlogModule.forRoot({ drain: createAxiomDrain() })
```
```typescript [Standalone]
import { createAxiomDrain } from 'evlog/axiom'

initLogger({ drain: createAxiomDrain() })
```
::

就是这样！你的日志现在会显示在 Axiom 中。

## 配置

该适配器从多个来源读取配置（优先级从高到低）：

1. **覆盖项** 传递给 `createAxiomDrain()`
2. **运行时配置** 位于 `runtimeConfig.axiom`（仅限 Nuxt/Nitro）
3. **环境变量**（`AXIOM_*`）

### 环境变量

| 变量 | 说明 |
|----------|-------------|
| `AXIOM_API_KEY` | 具有写入权限的 Axiom API 令牌 |
| `AXIOM_DATASET` | 用于导入日志的数据集名称 |
| `AXIOM_ORG_ID` | 组织 ID（个人访问令牌必填） |
| `AXIOM_EDGE_URL` | 用于导入/查询的边缘基础 URL（适用于边缘部署） |
| `AXIOM_URL` | API 基础 URL（旧版/默认导入端点） |

### 运行时配置（仅限 Nuxt）

通过 `nuxt.config.ts` 进行类型安全的配置：

```typescript [nuxt.config.ts]
export default defineNuxtConfig({
  runtimeConfig: {
    axiom: {
      apiKey: '', // Set via AXIOM_API_KEY
      dataset: '', // Set via AXIOM_DATASET
    },
  },
})
```

### 覆盖选项

直接传递选项以覆盖任何配置：

```typescript [server/plugins/evlog-drain.ts]
const drain = createAxiomDrain({
  dataset: 'production-logs',
  timeout: 10000,
})
```

### 完整配置参考

| 选项 | 类型 | 默认值 | 说明 |
|--------|------|---------|-------------|
| `apiKey` | `string` | - | Axiom API 令牌（必需） |
| `token` | `string` | - | **已弃用。** 请改用 `apiKey` |
| `dataset` | `string` | - | 数据集名称（必需） |
| `orgId` | `string` | - | 组织 ID（适用于 PAT 令牌） |
| `edgeUrl` | `string` | - | 用于摄取的 Edge URL。如果未提供路径，则使用 `/v1/ingest/{dataset}`；自定义路径将按原样使用（会去除末尾斜杠）。与 `baseUrl` 互斥 |
| `baseUrl` | `string` | `https://api.axiom.co` | API 基础 URL（`/v1/datasets/{dataset}/ingest`），与 `edgeUrl` 互斥 |
| `timeout` | `number` | `5000` | 请求超时时间，单位为毫秒 |

## 在 Axiom 中查询日志

evlog 发送结构化广泛事件，非常适合 Axiom 的 APL 查询语言：

```apl [Axiom APL 查询]
// 查找慢速请求
['your-dataset']
| where duration > 1000
| project timestamp, path, duration, status

// 按端点统计错误率
['your-dataset']
| where level == "error"
| summarize count() by path
| order by count_ desc

// 随时间统计请求量
['your-dataset']
| summarize count() by bin(timestamp, 1h)
| render timechart
```

## 故障排除

### 缺少数据集或 apiKey 错误

```text [Console]
[evlog/axiom] Missing dataset or apiKey. Set AXIOM_API_KEY/AXIOM_DATASET env vars or pass to createAxiomDrain()
```

请确保已设置环境变量并在添加后重新启动服务器。

### 401 未授权

你的令牌可能无效或已过期。在 Axiom 仪表板中生成一个具有 **写入** 权限的新令牌。

### 403 禁止（使用个人访问令牌）

个人访问令牌需要组织 ID：

```bash [.env]
AXIOM_ORG_ID=your-org-id
```

## 直接 API 使用

对于高级用例，你可以使用更低层级的函数：

```typescript [server/utils/axiom.ts]
import { sendToAxiom, sendBatchToAxiom } from 'evlog/axiom'

// 发送单个事件
await sendToAxiom(event, {
  apiKey: 'xaat-xxx',
  dataset: 'logs',
})

// 批量发送多个事件
await sendBatchToAxiom(events, {
  apiKey: 'xaat-xxx',
  dataset: 'logs',
})
```

## 下一步

- [OTLP 适配器](/integrate/adapters/cloud/otlp) - 通过 OpenTelemetry Protocol 发送日志
- [PostHog 适配器](/integrate/adapters/cloud/posthog) - 将日志发送到 PostHog
- [自定义适配器](/extend/custom-drains) - 构建你自己的适配器
- [最佳实践](/reference/best-practices) - 安全和生产建议
