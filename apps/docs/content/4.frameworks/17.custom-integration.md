---
title: 自定义集成
description: 使用 toolkit API 构建你自己的 evlog 框架集成——定义 defineFrameworkIntegration、createMiddlewareLogger、AsyncLocalStorage，以及完整的 drain/enrich/keep 管道。
navigation:
  title: 自定义集成
  icon: i-lucide-puzzle
---

没看到你的框架？`evlog/toolkit` 包暴露了与每个内置集成（Hono、Express、Fastify、Elysia、NestJS、SvelteKit）相同的构建模块。只需大约 30 行代码，就能为任何 HTTP 框架构建一个功能完整的 evlog 中间件。

::callout{icon="i-lucide-flask-conical" color="warning"}
工具包 API 被标记为 **beta**。其接口面已保持稳定（所有内置集成都在使用），但可能会根据社区反馈继续演进。
::

::prompt
---
description: 为我的框架构建一个 evlog 集成
icon: i-lucide-puzzle
actions:
  - copy
  - cursor
  - windsurf
---

使用 toolkit API 为我的自定义 HTTP 框架构建一个 evlog 集成。

- 安装 evlog：pnpm add evlog
- 从 'evlog/toolkit' 导入 { defineFrameworkIntegration, createLoggerStorage }（不是 'evlog/shared'）
- 传入一个 manifest：extractRequest(ctx) 返回 { method, path, headers, requestId? }，attachLogger(ctx, logger)，以及来自 createLoggerStorage() 的可选 storage
- headers 可以是 Web Headers 或 Node IncomingHttpHeaders — defineFrameworkIntegration 会统一规范化两者
- 在你的中间件中，调用 integration.start(ctx, options)，它会返回 { skipped, finish, runWith, logger, middlewareOptions }
- 如果 skipped 为 true，则直接跳过并调用 next
- 将下游处理器放在 runWith(() => next()) 中执行，这样 AsyncLocalStorage 和 log.fork() 才能正常工作
- 成功时：await finish({ status })；出错时：await finish({ error }) 然后重新抛出
- 暴露 drain、enrich、keep、include、exclude、routes 和 plugins 选项

文档：https://www.evlog.dev/frameworks/custom-integration
::

## 安装

::code-group
```bash [pnpm]
pnpm add evlog
```
```bash [bun]
bun add evlog
```
```bash [yarn]
yarn add evlog
```
```bash [npm]
npm install evlog
```
::

## 工具包里有什么

| 导出 | 用途 |
|--------|---------|
| `defineFrameworkIntegration(spec)` | Manifest 工厂——提取请求、创建 logger、附加 logger、在 ALS 中运行 |
| `createMiddlewareLogger(opts)` | 更底层的生命周期（自定义模式） |
| `BaseEvlogOptions` | 面向用户的基础选项——`drain`、`enrich`、`keep`、`include`、`exclude`、`routes`、`plugins` |
| `MiddlewareLoggerResult` | 返回类型：`{ logger, finish, skipped }` |
| `extractSafeHeaders(headers)` | 从 Web API `Headers` 对象中过滤敏感头信息（内部使用） |
| `extractSafeNodeHeaders(headers)` | 从 Node.js `IncomingHttpHeaders` 中过滤敏感头信息（内部使用） |
| `createLoggerStorage(hint)` | 返回由 `AsyncLocalStorage` 支持的 `{ storage, useLogger }` 工厂 |
| `defineEvlog(config)` | 规范化配置对象——适用于 `initLogger` 和中间件选项 |
| `definePlugin(plugin)` | 插件契约——可选择启用 `setup`、`enrich`、`drain`、`keep`、`onRequestStart`、`onRequestFinish`、`onClientLog`、`extendLogger` 的任意子集 |
| `composeEnrichers / composeDrains / composeKeep / composePlugins` | 将多个扩展组合成一个 |
| `defineEnricher(spec)` | 构建单字段 enricher，并内置错误隔离 |
| `defineHttpDrain(spec)` | 构建 HTTP drain——提供 `resolve()` 和 `encode()`，即可免费获得重试/超时/错误处理 |
| `httpPost(opts)` | 每个内置适配器都在使用的同款重试 POST 辅助函数 |
| `resolveAdapterConfig(ns, fields, overrides)` | 标准配置优先级链——覆盖项 → runtimeConfig → env |

像 `RequestLogger`、`DrainContext`、`EnrichContext`、`WideEvent` 和 `TailSamplingContext` 这样的类型都从主 `evlog` 包中导出。

## Manifest 模式（推荐）

大多数框架都适合 `(ctx, next)` 这种中间件形状。对于这些框架，写一个描述如何提取请求和附加 logger 的 **manifest** 即可——剩下的交给 `defineFrameworkIntegration`。

```typescript [my-framework-evlog.ts]
import type { IncomingMessage, ServerResponse } from 'node:http'
import {
  createLoggerStorage,
  defineFrameworkIntegration,
  type BaseEvlogOptions,
} from 'evlog/toolkit'
import type { RequestLogger } from 'evlog'

export type MyFrameworkEvlogOptions = BaseEvlogOptions

const { storage, useLogger } = createLoggerStorage(
  'middleware context. Make sure evlog middleware is registered before your routes.',
)

export { useLogger }

const integration = defineFrameworkIntegration<IncomingMessage>({
  name: 'my-framework',
  extractRequest: (req) => ({
    method: req.method || 'GET',
    path: req.url || '/',
    headers: req.headers,
    requestId: typeof req.headers['x-request-id'] === 'string'
      ? req.headers['x-request-id']
      : undefined,
  }),
  attachLogger: (req, logger) => {
    (req as IncomingMessage & { log: RequestLogger }).log = logger
  },
  storage,
})

export function evlog(options: MyFrameworkEvlogOptions = {}) {
  return async (req: IncomingMessage, res: ServerResponse, next: () => Promise<void>) => {
    const { skipped, finish, runWith } = integration.start(req, options)
    if (skipped) {
      await next()
      return
    }
    try {
      await runWith(() => next())
      await finish({ status: res.statusCode })
    } catch (error) {
      await finish({ error: error as Error })
      throw error
    }
  }
}
```

就是这样。这个中间件可免费获得**所有特性**：路由过滤、drain 适配器、enricher、尾部采样、错误捕获、插件生命周期钩子、`log.fork()`，以及时长跟踪。

## `defineFrameworkIntegration` 的工作方式

基于上面的 manifest，这个辅助函数会：

1. 规范化 headers（自动识别 `Headers` 与 `IncomingHttpHeaders`）。
2. 如果 `extractRequest` 没有返回 `requestId`，则自动生成一个。
3. 使用合并后的选项调用 `createMiddlewareLogger`。
4. 调用 `attachLogger(ctx, logger)`。
5. 当提供了 `storage` 时，为 logger 附加 `log.fork()`（这样用户就能发起相关联的后台工作）。
6. 暴露 `runWith(fn)`——如果配置了 `storage`，就会在 `storage.run(logger, …)` 中运行 `fn()`，否则直接调用 `fn()`。

你最终只需要处理框架相关的胶水代码：从哪里读取请求、在哪里附加 logger，以及如何计算响应状态。

## 自定义模式

如果你的框架生命周期不适合清晰的 `(ctx, next)` 结构（NestJS 拦截器、Next.js App Router、SvelteKit `handle`），那就再往下一层，直接调用 `createMiddlewareLogger`：

```typescript
import { createMiddlewareLogger, extractSafeNodeHeaders } from 'evlog/toolkit'

const { logger, finish, skipped } = createMiddlewareLogger({
  method,
  path,
  requestId,
  headers: extractSafeNodeHeaders(rawHeaders),
  ...options,
})
```

你需要负责 ALS 包装（`storage.run`）、`log.fork()` 附加（通过 `attachForkToLogger`），以及完成生命周期——但你仍然可以免费获得完整管道（路由过滤、采样、emit、enrich、drain、插件）。

## 参考实现

研究这些内置集成，了解不同框架的模式：

| 框架 | 行数 | 模式 | 源码 |
|-----------|-------|------|--------|
| Hono | ~50 | manifest | [hono/index.ts](https://github.com/hugorcd/evlog/blob/main/packages/evlog/src/hono/index.ts) |
| Express | ~50 | manifest + ALS | [express/index.ts](https://github.com/hugorcd/evlog/blob/main/packages/evlog/src/express/index.ts) |
| Fastify | ~70 | manifest + Fastify hooks | [fastify/index.ts](https://github.com/hugorcd/evlog/blob/main/packages/evlog/src/fastify/index.ts) |
| Elysia | ~80 | manifest + 自定义 ALS 作用域 | [elysia/index.ts](https://github.com/hugorcd/evlog/blob/main/packages/evlog/src/elysia/index.ts) |
| NestJS | ~120 | 自定义（拦截器） | [nestjs/](https://github.com/hugorcd/evlog/blob/main/packages/evlog/src/nestjs/) |
| SvelteKit | ~90 | 自定义（`handle` 钩子） | [sveltekit/](https://github.com/hugorcd/evlog/blob/main/packages/evlog/src/sveltekit/) |

::callout{icon="i-lucide-heart" color="neutral"}
为我们不支持的框架构建了一个集成？[提交 PR](https://github.com/hugorcd/evlog/pulls)——社区会感谢你的。
::

## 下一步

- [工具包参考](/adapters/building-blocks/custom)：将所有工具包原语汇集在一处
- [宽事件](/logging/wide-events)：通过上下文分层设计完整事件
- [适配器](/adapters/overview)：将日志发送到 Axiom、Sentry、PostHog 等更多平台
- [采样](/core-concepts/sampling)：通过头部采样和尾部采样控制日志量
- [结构化错误](/logging/structured-errors)：抛出带有 `why`、`fix` 和 `link` 字段的错误
