---
title: 遥测摄取
description: 为 @evlog/telemetry 构建一个服务器摄取端点——半公开威胁模型、parseIngestBody 验证、框架路由、幂等存储和限流。
navigation:
  title: 摄取
  icon: i-lucide-server
links:
  - label: 概览
    icon: i-lucide-list
    to: /use-cases/telemetry/overview
    color: neutral
    variant: subtle
  - label: 设置
    icon: i-lucide-wrench
    to: /use-cases/telemetry/setup
    color: neutral
    variant: subtle
  - label: 参考
    icon: i-lucide-book-open
    to: /use-cases/telemetry/reference
    color: neutral
    variant: subtle
---

你发布了一个 CLI。用户在他们的机器上运行它。你想知道 *哪些命令被执行、失败频率如何，以及有哪些版本在外部流通* —— 而不必构建一个单独的分析 SDK 或读取原始 `argv`。

这意味着你需要一个 **服务器端点**，由你的 CLI 向其发送 POST 请求。本地 outbox 只是为了让短暂的 CI 作业和离线运行在 POST 成功之前不会丢失数据。

## 安全问题（先读这个）

::tip
**CLI 遥测不是保险库。** 你收集的是匿名使用计数器（命令名、持续时间、结果、少量数值）——不是密码、不是文件路径、不是令牌。按这个威胁模型来设计，你就不需要不可能实现的客户端密钥。
::

**“我把 ingest URL 放进了我的 CLI 里——这难道不是秘密吗？”**

不是。这个 URL 存在于你的 npm 包或二进制文件中。任何人都可以用 `strings`、通过安装该包，或者通过监视网络流量来读取它。把这个道理同样应用到硬编码进 CLI 的 API key 或 `Authorization` header 上——它们都可以被提取，不是真正的防护。

**“那不是任何人都能发送伪造事件吗？”**

理论上是的——有人可以用伪造的 JSON `curl` 你的端点。这就是为什么你要把该端点视为**半公开**的（就像浏览器分析 key 一样），并在服务器端防御：

| 你可能担心的事 | 实际做法 |
| --- | --- |
| 随机互联网机器人 | 校验 payload 结构；对垃圾数据返回 `400` |
| 有人疯狂刷假的 `doctor` 运行 | 按 IP 限流；数据只是低价值计数器，不是钱 |
| 伪造的 `tool.name` | 服务器端只允许你已发布的工具名称白名单 |
| `custom` 中出现意外字段 | 只过滤为你声明过的 key——和 CLI 端 `collect` 的列表保持一致 |
| 重试导致重复计数 | 存储时按 `idempotencyKey` 去重 |
| 泄露用户秘密 | CLI 从不读取原始 `argv`；你只允许列出的 flags 和 `telemetry.set()` 计数器 |

**“足够好”是什么样子：** 校验每个 POST，幂等存储，在边缘做限流（CDN、API gateway 或中间件），并让 payload 保持简单（设计上不包含 PII）。这和客户端分析的做法是一样的——不是银行级认证，但足以获得可靠的产品洞察。

::callout{icon="i-lucide-shield-check" color="info"}
`@evlog/telemetry` 为你的服务器提供了 **`parseIngestBody()`** ——和这里文档中描述的校验规则一致，因此你不需要从文档里复制粘贴一个验证器。把它与你框架的路由，以及你的数据库或 evlog drain 组合使用。
::

## 第 1 步 — 使用 `parseIngestBody()` 进行验证

在服务器上镜像 CLI 配置：相同的 `name`，以及你通过 `collect` / `telemetry.set()` 允许的相同自定义键。

```ts [lib/telemetry-ingest.ts]
import { parseIngestBody } from '@evlog/telemetry/ingest'

export const ingestOptions = {
  allowedTools: ['my-tool'],
  allowedCustomKeys: {
    'my-tool': ['checksFailed', 'checksWarn', 'itemsSynced'],
  },
} as const

export function parseTelemetryBody(raw: string) {
  return parseIngestBody(raw, ingestOptions)
}
```

`parseIngestBody()` 会检查批量大小、JSON 结构、`event: 'run'`、工具允许列表、envelope 类型、ISO 时间戳，并移除你未声明的 `custom` 键。失败时会抛出 `IngestValidationError` — 请在你的路由中返回 `400`。

## 第 2 步 — 连接你的路由

对任何框架都适用的相同契约——读取原始请求体、校验、存储，然后返回 **204**，这样 CLI 就会清空它的 outbox：

::code-group
```ts [server/api/telemetry/ingest.post.ts (Nitro)]
import { defineEventHandler, readRawBody, setResponseStatus } from 'h3'
import { IngestValidationError } from '@evlog/telemetry/ingest'
import { parseTelemetryBody } from '~/lib/telemetry-ingest'
import { storeRunEvents } from '~/lib/telemetry-store'

export default defineEventHandler(async (event) => {
  const raw = await readRawBody(event, 'utf8')
  if (!raw) {
    setResponseStatus(event, 400)
    return { error: '请求体为空' }
  }

  try {
    const events = parseTelemetryBody(raw)
    await storeRunEvents(events)
    setResponseStatus(event, 204)
    return null
  } catch (err) {
    setResponseStatus(event, err instanceof IngestValidationError ? 400 : 500)
    return { error: '无效载荷' }
  }
})
```
```ts [app/api/telemetry/ingest/route.ts (Next.js App Router)]
import { IngestValidationError } from '@evlog/telemetry/ingest'
import { parseTelemetryBody } from '@/lib/telemetry-ingest'
import { storeRunEvents } from '@/lib/telemetry-store'

export async function POST(request: Request) {
  const raw = await request.text()
  try {
    const events = parseTelemetryBody(raw)
    await storeRunEvents(events)
    return new Response(null, { status: 204 })
  } catch (err) {
    const status = err instanceof IngestValidationError ? 400 : 500
    return Response.json({ error: '无效载荷' }, { status })
  }
}
```
```ts [src/routes/telemetry.ts (Hono)]
import { Hono } from 'hono'
import { IngestValidationError } from '@evlog/telemetry/ingest'
import { parseTelemetryBody } from '../lib/telemetry-ingest'
import { storeRunEvents } from '../lib/telemetry-store'

const app = new Hono()

app.post('/api/telemetry/ingest', async (c) => {
  const raw = await c.req.text()
  try {
    const events = parseTelemetryBody(raw)
    await storeRunEvents(events)
    return c.body(null, 204)
  } catch (err) {
    const status = err instanceof IngestValidationError ? 400 : 500
    return c.json({ error: '无效载荷' }, status)
  }
})

export default app
```
::

## 第 3 步 — 幂等存储

重试和积压清理可能会用相同的 `idempotencyKey` 发送两次 POST。使用 upsert（或跳过）以确保指标保持正确：

```ts [lib/telemetry-store.ts]
import type { RunEvent } from '@evlog/telemetry'

export async function storeRunEvents(events: RunEvent[]): Promise<void> {
  for (const run of events) {
    await db.telemetryRun.upsert({
      where: { idempotencyKey: run.idempotencyKey },
      create: {
        tool: run.tool.name,
        version: run.tool.version,
        command: run.command,
        outcome: run.outcome,
        durationMs: run.durationMs,
        custom: run.custom,
        recordedAt: run.timestamp,
      },
      update: {},
    })
  }

  // 或者转发到 evlog 的 drain pipeline，供 Axiom / Datadog / OTLP 使用：
  // await ingestWideEvents(events.map(run => ({ source: 'telemetry', ...run })))
}
```

## 第 4 步 — 在边缘进行速率限制

`parseIngestBody()` 是你的最后一道 schema 防线，但不是唯一一道。请在流量进入的位置添加速率限制——CDN、API 网关或中间件——尤其是按 IP 进行限制。可选：如果你需要更严格的控制，也可以在处理程序内部按 `machineId` 哈希进行分桶。

非 2xx 响应会将事件保留在用户的 outbox 中；它们会在下一次 CLI 调用时重试。只有在存储成功时才返回 **204**（或任何 `2xx`）。
