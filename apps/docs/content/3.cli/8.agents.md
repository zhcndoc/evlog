---
title: evlog 代理
description: 教会在您的代码仓库中工作的 AI 代理如何使用 evlog — 在 AGENTS.md 中添加一小段约定，以及通过 npx skills 安装已发布的代理技能。
navigation:
  title: 代理
  icon: i-lucide-bot
links:
  - label: 代理技能
    icon: i-lucide-sparkles
    to: /reference/agent-skills
    color: neutral
    variant: subtle
---

将 evlog 接入应用只是工作的一半。另一半是由助手编写处理程序，而它会不断使用 `console.log` 和 `throw new Error(...)`，直到代码仓库中的某些内容告诉它不要这样做。

`evlog agents` 会写入这部分内容。

```bash [Terminal]
evlog agents
```

```text [Output]
nuxt

✓ created AGENTS.md
✓ created CLAUDE.md
✓ installed the evlog skills
  ran      npx --yes skills add https://www.evlog.dev
```

## 写入内容

| 文件 | 处理方式 |
| --- | --- |
| `AGENTS.md` | 如果文件不存在则创建。否则，在 `<!-- evlog:start -->` 和 `<!-- evlog:end -->` 之间原地替换 evlog 块——这些标记之外的所有内容都保持完全不变。 |
| `CLAUDE.md` | 如果文件不存在，则创建为单行内容 `@AGENTS.md`。如果文件已存在且已经提到 `AGENTS.md`，则保持不变。 |

该代码块有意保持简短——它会在每次代理交互中加载。它说明了相关规则（每次操作记录一个宽事件、对上下文进行分组、使用结构化错误、对敏感操作进行审计，以及明确哪些内容永远不会被记录），并指向相关技能以获取详细信息。其中命名的日志记录器访问方式遵循你的框架：Nuxt 和 Nitro 使用 `useLogger(event)`，Next.js 使用 `lib/evlog.ts` 中的 `useLogger()`，TanStack Start 使用 `req.context.log`。如果未检测到框架，仍会使用通用访问方式写入该代码块——这些约定同样适用于 Express 或 Hono。

## 技能不是由我们来安装的

[代理技能](/reference/agent-skills)来自 [`npx skills`](https://github.com/vercel-labs/skills)，`evlog agents` 会调用它，就像 [`evlog init`](/cli/init) 运行你的包管理器，而不是自行解压 tarball 一样。

这是有意为之的。每个代理读取不同的目录（`.claude/skills`、`.agents/skills`、`.codex/skills`，……），而 skills CLI 已经会针对每个代理解析这些目录、链接一个规范副本，并支持全局范围。它也不会保留清单，因此我们暗中写入的任何副本都会变成它永远无法更新的第二份副本。委托给它意味着只保留一份副本，并且 `npx skills update` / `remove` / `list` 仍然可以正常操作。

在运行任何操作之前，`evlog agents` 会查找已经安装的 evlog 技能——无论属于哪个代理、项目本地还是全局——如果找到，就会保留它们不变：

```text [Output]
· AGENTS.md is up to date
· CLAUDE.md already points at AGENTS.md
✓ skills already installed · .agents/skills, .claude/skills
   npx skills update to refresh them
```

::callout{icon="i-lucide-info" color="neutral"}
在交互模式下，skills CLI 会询问它自己的问题——要为哪些代理安装，以及安装到项目范围还是全局范围。这个问题应该由它来询问，因此 `evlog agents` 会把终端交给它，而不是代替你回答。在非交互运行（`--json`、`--yes`、CI、没有 TTY）中，会传入 `--yes`，这样就不会因为无人回答提示而阻塞。
::

### 你所信任的内容

委托执行意味着运行第三方代码，因此有必要明确说明信任模型。

- **会运行什么。** 始终只有一条命令，并且绝不会从你未传入的任何内容中拼接：`npx --yes skills add <source>`；当运行是非交互式时，还会追加 `--yes`，而当你要求使用相应选项时，则会追加 `--skill` / `--global`。在交互模式下，它会显示两次——一次是在你确认的计划中，另一次是在步骤开始时。以非交互方式（`--json`、`--yes`、CI、没有 TTY）运行时，它会出现在运行结束后打印的报告中。无论哪种方式，该字符串都会逐参数作为命令执行，因此重新输入它即可完全复现此次运行；`--dry-run` 会显示它但不执行任何操作。
- **未固定版本。** `npx` 会在运行时解析最新的 [`skills`](https://www.npmjs.com/package/skills)。这正是这样设计的目的——技能指南跟随文档网站，而不是 CLI 版本发布——但这也意味着你今天获得的版本可能与上个月获得的版本不同。如果你的策略要求固定版本，请使用 `--no-skills`，然后自行运行固定版本的 `npx skills@<version> add https://www.evlog.dev`。
- **会验证 `--source`。** 它必须是一个普通的 `http:` / `https:` 源，只能包含字母、数字和 `. _ ~ : / -`，因此不能包含查询字符串或 shell 元字符；而 `--skills` 条目必须是小写且以连字符分隔的名称。在 Windows 上，启动进程需要 shell 才能解析 `npx`（Node 不会在没有 shell 的情况下运行 `.cmd`），因此这两项都会在启动任何进程之前完成检查，而不是将信任交给后续链路。
- **未经决定不会启动任何进程。** `--no-skills` 会完全跳过该步骤，而交互流程在你确认计划之前也不会执行到该命令。

如果这些都不符合你的策略，`evlog agents --no-skills` 仍然会写入 `AGENTS.md` 和 `CLAUDE.md`——它们绝不会访问网络——你可以按照自己偏好的方式安装技能。

## 可安全重复运行

再次运行会根据当前 CLI 刷新代码块。任何已经完全相同的内容都会被报告，而不会被重写，因此第二次运行会保持工作树干净。在升级 `@evlog/cli` 后运行它。

## 标志

| 标志 | 功能 |
| --- | --- |
| `--skills <list>` | 以逗号分隔的技能名称，传递给 `npx skills add --skill`（默认为全部技能） |
| `--no-skills` | 仍然写入 `AGENTS.md` 和 `CLAUDE.md`；仅跳过技能安装 — 不会启动任何进程 |
| `--global`, `-g` | 为所有项目安装技能，而不仅仅是当前项目 |
| `--source <url>` | 技能发布地址 — 一个纯 http(s) 源（默认为 `https://www.evlog.dev`） |
| `--dry-run` | 显示计划，但不写入或运行任何内容 |
| `--yes`, `-y` | 无需确认直接应用 |

```bash [Terminal]
evlog agents --skills review-logging-patterns
evlog agents --global
evlog agents --no-skills
evlog agents --dry-run
```

如果技能 CLI 失败 — 没有网络或没有 `npx` — 该代码块仍会保留在磁盘上，并且命令会报告失败并以 1 退出。`AGENTS.md` 代码块始终不需要网络。

## 作为 `evlog init` 的一部分

[`evlog init`](/cli/init) 将此作为最后一个问题。`AGENTS.md` 和 `CLAUDE.md` 的写入会与 evlog 接入安排在同一计划中——一个列表，一次确认——技能则会与包管理器安装同时运行。使用 `--no-agents` 跳过：

```bash [Terminal]
evlog init --no-agents
```

## 后续步骤

- [Agent Skills](/reference/agent-skills) — 每项技能的教学内容
- [`evlog map`](/cli/map) — 评估你的代理仍未覆盖的内容
- [宽事件](/learn/wide-events) — 该区块总结的约定
