---
title: 接入 evlog
description: 选择一个框架集成来捕获请求，然后选择适配器来发送事件。框架决定日志记录器的位置，适配器决定事件的去向。
navigation:
  title: 概览
  icon: i-lucide-plug
links:
  - label: 框架
    icon: i-lucide-layers
    to: /integrate/frameworks/overview
    color: neutral
    variant: subtle
  - label: 适配器
    icon: i-custom-plug
    to: /integrate/adapters/overview
    color: neutral
    variant: subtle
---

在你理解了 [日志模式](/learn/overview) 之后，在将 evlog 接入你的技术栈之前，还剩两个问题需要回答：

1. **日志记录器位于哪里？** → 选择一个[框架集成](/integrate/frameworks/overview)。每个集成都会在每个请求上创建日志记录器，将其附加到上下文中，并在响应结束时发出宽事件。你无需管理其生命周期。
2. **事件会去哪里？** → 选择一个或多个[适配器](/integrate/adapters/overview)。适配器会将宽事件发送到外部可观测性平台：Axiom、Datadog、Sentry、PostHog、兼容 OTLP 的系统、文件系统、NuxtHub。

这两者彼此独立。Nuxt 应用可以输出到 Axiom，Express 应用可以同时输出到 OTLP + Sentry，SvelteKit 应用在开发环境中可以输出到本地文件，在生产环境中可以输出到 Datadog。

接入完成后，运行 [`evlog map`](/cli/map)（`npx @evlog/cli map`）来查看哪些入口点仍然没有记录，或者如果没有任何内容显示，可以运行 [`evlog doctor`](/cli/doctor)。这是一个独立的早期包；可选，但值得运行一次。

::card-group
  :::card
  ---
  icon: i-lucide-layers
  title: 框架（16）
  to: /integrate/frameworks/overview
  color: neutral
  ---
  Nuxt、Next.js、SvelteKit、Nitro、TanStack Start、NestJS、Express、Hono、Fastify、Elysia、React Router、Cloudflare Workers、AWS Lambda、Astro、Standalone、自定义集成。
  :::

  :::card
  ---
  icon: i-custom-plug
  title: 适配器（9）
  to: /integrate/adapters/overview
  color: neutral
  ---
  云端目的地（Axiom、OTLP、HyperDX、PostHog、Sentry、Better Stack、Datadog）以及自托管（文件系统、NuxtHub）。
  :::
::

## 没有找到你的框架？

查看[自定义框架集成](/extend/custom-framework)。`evlog/toolkit` 包公开了每个内置集成所使用的相同构建模块。大多数 HTTP 框架只需要大约 30 行连接代码。

## 没有找到你的目标端？

查看 [自定义 Drain](/extend/custom-drains)。来自 `evlog/toolkit` 的 `defineHttpDrain` 只需一个函数就能将任意后端接入，并免费为你提供批处理、重试、超时和身份标头。
