---
title: Grafana Loki 适配器
description: 将宽事件推送到 Grafana Loki——自托管、多租户或 Grafana Cloud——使用低基数标签并支持完整的 JSON 查询。
navigation:
  title: Grafana Loki
  icon: i-simple-icons-grafana
links:
  - label: Loki HTTP API
    icon: i-lucide-external-link
    to: https://grafana.com/docs/loki/latest/reference/loki-http-api/
    target: _blank
    color: neutral
    variant: subtle
  - label: LogQL
    icon: i-lucide-external-link
    to: https://grafana.com/docs/loki/latest/query/
    target: _blank
    color: neutral
    variant: subtle
---

Loki 适配器通过其推送 API 将宽事件推送到 [Grafana Loki](https://grafana.com/docs/loki/latest/)。它适用于自托管的单租户实例、多租户部署以及 Grafana Cloud。

每个事件都会作为一行 **JSON 日志** 推送，并使用一组精简、低基数的标签。在 Loki 中，这一区别非常重要：标签会根据基数建立索引并计费，而日志行则在查询时进行搜索。evlog 默认仅标记 `service`、`environment` 和 `level`，其余所有内容——`requestId`、`path`、`user` 以及你的自定义字段——都会保留在日志行中，可通过 `| json` 进行查询。

::prompt
---
description: 添加 Grafana Loki drain 适配器
icon: i-simple-icons-grafana
actions:
  - copy
  - cursor
  - claude
---

添加 Grafana Loki drain 适配器，将 evlog 宽事件发送到 Loki。

1. 识别我使用的框架，并遵循该框架的 evlog 集成模式
2. 如果尚未安装 evlog，则安装它
3. 从 'evlog/loki' 导入 createLokiDrain
4. 将 createLokiDrain() 接入我所用框架的 drain 配置
5. 设置 LOKI_ENDPOINT（例如 http://localhost:3100）。对于 Grafana Cloud，还需设置 LOKI_USER（实例 ID）和 LOKI_API_KEY
6. 对于多租户自托管 Loki，则设置 LOKI_TENANT_ID
7. 通过触发请求并在 Grafana 中查询 {service="my-app"} 进行测试

适配器文档：https://www.evlog.dev/integrate/adapters/hybrid/loki
框架设置：https://www.evlog.dev/integrate/frameworks/overview

::

## 安装

Loki 适配器已随 evlog 一起提供：

```typescript [src/index.ts]
import { createLokiDrain } from 'evlog/loki'
```

## 快速开始

设置 `LOKI_ENDPOINT`，并将日志排出器接入你的框架。

::code-group
```typescript [Nuxt / Nitro]
// server/plugins/evlog.ts
import { createLokiDrain } from 'evlog/loki'

export default defineNitroPlugin((nitroApp) => {
  nitroApp.hooks.hook('evlog:drain', createLokiDrain())
})
```

```typescript [Hono]
import { Hono } from 'hono'
import { evlog } from 'evlog/hono'
import { createLokiDrain } from 'evlog/loki'

const app = new Hono()
app.use(evlog({ drain: createLokiDrain() }))
```

```typescript [Express]
import express from 'express'
import { evlog } from 'evlog/express'
import { createLokiDrain } from 'evlog/loki'

const app = express()
app.use(evlog({ drain: createLokiDrain() }))
```

```typescript [Fastify]
import Fastify from 'fastify'
import { evlog } from 'evlog/fastify'
import { createLokiDrain } from 'evlog/loki'

const app = Fastify()
await app.register(evlog, { drain: createLokiDrain() })
```

```typescript [Elysia]
import { Elysia } from 'elysia'
import { evlog } from 'evlog/elysia'
import { createLokiDrain } from 'evlog/loki'

const app = new Elysia().use(evlog({ drain: createLokiDrain() }))
```

```typescript [NestJS]
import { Module } from '@nestjs/common'
import { EvlogModule } from 'evlog/nestjs'
import { createLokiDrain } from 'evlog/loki'

@Module({
  imports: [EvlogModule.forRoot({ drain: createLokiDrain() })],
})
export class AppModule {}
```

```typescript [Standalone]
import { initLogger } from 'evlog'
import { createLokiDrain } from 'evlog/loki'

initLogger({
  env: { service: 'my-app' },
  drain: createLokiDrain(),
})
```
::

## 配置

### 环境变量

| 变量 | 必填 | 描述 |
|----------|----------|-------------|
| `LOKI_ENDPOINT` | 是 | Loki 实例的基础 URL，不包含推送路径（也接受 `LOKI_URL`） |
| `LOKI_API_KEY` | 否 | API 令牌。设置了 `LOKI_USER` 时以 Basic 方式发送，否则以 Bearer 方式发送（也接受 `GRAFANA_API_KEY`） |
| `LOKI_USER` | 否 | Grafana Cloud 实例 ID。将身份验证切换为 Basic（也接受 `GRAFANA_USER`） |
| `LOKI_TENANT_ID` | 否 | 多租户自托管 Loki 的租户，以 `X-Scope-OrgID` 发送 |

### 优先级

配置按从高到低的优先级解析：

1. 传递给 `createLokiDrain()` 的覆盖配置
2. `runtimeConfig.evlog.loki`（Nitro）
3. `runtimeConfig.loki`（Nitro）
4. 环境变量

### 选项

| 选项 | 类型 | 默认值 | 描述 |
|--------|------|---------|-------------|
| `endpoint` | `string` | — | 基础 URL，不包含 `/loki/api/v1/push` |
| `apiKey` | `string` | — | API 令牌 |
| `user` | `string` | — | Grafana Cloud 实例 ID（启用 Basic 身份验证） |
| `tenantId` | `string` | — | 多租户 Loki 使用的 `X-Scope-OrgID` |
| `labelFields` | `string[]` | `['service', 'environment', 'level']` | 提升为 Loki 标签的事件字段 |
| `labels` | `Record<string, string>` | — | 合并到每个流中的静态标签 |
| `timeout` | `number` | `5000` | 请求超时时间，单位为毫秒 |
| `retries` | `number` | `2` | 发生瞬时故障时的重试次数 |

## 部署

Loki 可以以任一方式运行，适配器在两种情况下都相同——只有身份验证方式不同。

### 自托管

单租户实例只需要端点：

```typescript [server/plugins/evlog.ts]
createLokiDrain({ endpoint: 'http://localhost:3100' })
```

```bash [.env]
LOKI_ENDPOINT=http://localhost:3100
```

对于**多租户**部署，请指定租户名称——它会作为 `X-Scope-OrgID` 发送：

```typescript [server/plugins/evlog.ts]
createLokiDrain({
  endpoint: 'http://loki.internal:3100',
  tenantId: 'team-checkout',
})
```

```bash [.env]
LOKI_ENDPOINT=http://loki.internal:3100
LOKI_TENANT_ID=team-checkout
```

如果你的实例位于需要身份验证的代理之后，则仅发送 `apiKey`，格式为
`Authorization: Bearer`。

### Grafana Cloud

Grafana Cloud 使用你的**实例 ID**和访问策略令牌进行身份验证，并将二者一起作为 HTTP Basic 发送：

```typescript [server/plugins/evlog.ts]
createLokiDrain({
  endpoint: 'https://logs-prod-eu-west-0.grafana.net',
  user: '123456',
  apiKey: process.env.GRAFANA_API_KEY,
})
```

```bash [.env]
LOKI_ENDPOINT=https://logs-prod-eu-west-0.grafana.net
LOKI_USER=123456
LOKI_API_KEY=glc_xxx
```

::callout{icon="i-lucide-info" color="info"}
`user` 是 Loki 数据源的**数字实例 ID**——在 Grafana Cloud 堆栈中通过
*连接 → 数据源 → Loki* 找到它。它不是你的账户邮箱。使用错误的值通常会导致
`401`。
::

### 应用哪种身份验证

| `user` | `apiKey` | `tenantId` | 发送的请求头 |
|---|---|---|---|
| ✓ | ✓ | | `Authorization: Basic base64(user:apiKey)` |
| | ✓ | | `Authorization: Bearer <apiKey>` |
| | | ✓ | `X-Scope-OrgID: <tenantId>` |
| | | | 无——未进行身份验证的实例 |

`tenantId` 独立存在，可以与任一身份验证模式结合使用。

## 标签与基数

::callout{icon="i-lucide-triangle-alert" color="warning"}
切勿将高基数字段提升为标签。Loki 会为每种唯一的标签组合创建一个流——为 `requestId` 或 `userId` 添加标签将创建数百万个流，并导致你的实例性能下降甚至崩溃。
::

仅为你会进行筛选且取值集合有界的字段添加标签：

```typescript [server/plugins/evlog.ts]
createLokiDrain({
  // `region` 的取值只有少数几个——安全
  labelFields: ['service', 'environment', 'level', 'region'],
  // 此部署中的所有内容使用的静态标签
  labels: { cluster: 'prod-eu' },
})
```

其他所有内容都会保留在 JSON 行中，并且完全支持查询——请参阅下文。

## Grafana 中的查询

按标签筛选，然后使用 `| json` 深入查看宽事件：

```logql
{service="checkout", environment="production"}
  | json
  | status >= 500
```

```logql
# 某个路由上的慢请求
{service="checkout"} | json | path="/api/orders" | durationMs > 1000
```

```logql
# 端到端的单个请求
{service="checkout"} | json | requestId="4a8ff3a8-..."
```

```logql
# 每个服务的错误率
sum by (service) (rate({environment="production", level="error"}[5m]))
```

## 批处理

将 drain 与管道结合使用，以批量推送，而不是逐个请求推送：

```typescript [server/plugins/evlog.ts]
import { createDrainPipeline } from 'evlog/pipeline'
import { createLokiDrain } from 'evlog/loki'
import type { DrainContext } from 'evlog'

const pipeline = createDrainPipeline<DrainContext>({
  batch: { size: 100, intervalMs: 5000 },
})

export default defineNitroPlugin((nitroApp) => {
  nitroApp.hooks.hook('evlog:drain', pipeline(createLokiDrain()))
})
```

共享同一标签集的事件会被分组到单个 Loki 流中，并按时间戳排序 — Loki 会拒绝同一流中乱序推送的条目。

## 在本地验证

在 Docker 中启动 Loki 并推送一个真实事件——无需云账户：

```bash [Terminal]
docker compose -f packages/evlog/test/e2e/docker-compose.yml up -d
LOKI_ENDPOINT=http://localhost:3100 pnpm run test:e2e
docker compose -f packages/evlog/test/e2e/docker-compose.yml down -v
```

该测试套件是一次**往返测试**：它推送事件，通过 Loki 的范围 API 将其查询回来，并断言标签集、JSON 日志行和时间戳均得以保留。如果未设置 `LOKI_ENDPOINT`，它会显示一个明确的标签并跳过自身，而不是静默通过。

如果想直接查看结果，可以将任意 Grafana 指向 `http://localhost:3100`，然后运行 `{service="evlog-e2e"}`。

## 故障排查

**`Missing endpoint`** — 未设置 `LOKI_ENDPOINT`，且未传入 `endpoint`。该 drain 会记录一次 `[evlog/loki] Missing endpoint`，随后不再执行任何操作，因此请求永远不会被阻止或失败。

**`Loki API error: 401`** — 在 Grafana Cloud 上，请确认 `user` 是 Loki 数据源的数字**实例 ID**，而不是你的账户邮箱。

**`Loki API error: 400`，提示条目顺序错误** — 较旧版本的 Loki 会拒绝早于流中最新条目的条目。evlog 会在每次推送中进行排序；如果仍然遇到此问题，请在 Loki 中启用 `unordered_writes`，或缩短批处理间隔。

**Grafana 中没有显示任何内容** — 确认你查询的标签集。先运行 `{service=~".+"}`，查看哪些流已到达。

## 直接使用 API

绕过 drain，自行推送事件：

```typescript [scripts/backfill.ts]
import { sendBatchToLoki, sendToLoki } from 'evlog/loki'

await sendToLoki(event, { endpoint: 'http://localhost:3100' })
await sendBatchToLoki(events, { endpoint: 'http://localhost:3100' })
```

`buildLokiPayload()`、`toLokiLabels()` 和 `resolveLokiPushUrl()` 也已导出，可用于自定义传输。

## 后续步骤

- [采样](/learn/sampling) — 在日志到达 Loki 之前控制日志量
- [增强器](/use-cases/enrichers) — 为每个事件添加派生字段
- [管道](/learn/pipeline) — 批处理、重试和扇出
- [适配器概览](/integrate/adapters/overview) — 所有可用的目标目的地
