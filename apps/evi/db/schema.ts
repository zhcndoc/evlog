// Drizzle schema for Evi's store. Tables are added by the features that need
// them; the binding object exists so `drizzle(client, { schema })` in
// `agent/lib/db.ts` is typed against whatever tables exist. Kept empty while
// the database is provisioned but nothing stores in it yet.
export const schema = {}
