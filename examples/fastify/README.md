# evlog — Fastify Example

Demonstrates evlog's Fastify plugin with wide events, structured errors, and PostHog drain.

## Run

```bash
# From the monorepo root
bun run example:fastify
```

Open [http://localhost:3000](http://localhost:3000) to test routes via the interactive UI.
