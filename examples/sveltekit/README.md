# evlog — SvelteKit Example

Demonstrates evlog's SvelteKit hooks with wide events, structured errors, and PostHog drain.

## Run

```bash
# From the monorepo root
bun run example:sveltekit
```

Open [http://localhost:5173](http://localhost:5173) to test routes via the interactive UI.
